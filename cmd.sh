# Run docker containers
docker-compose up -d

# List running containers
docker ps

# Go inside the container vm_one
docker exec -it vm_one /bin/bash

# Go inside the container vm_two
#docker exec -it vm_two /bin/bash


